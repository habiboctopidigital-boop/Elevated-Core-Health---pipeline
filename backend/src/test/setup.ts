// Vitest global setup (referenced by vite.config.mts `test.setupFiles`).
// No test file existed to exercise this config before phase 2's unit tests
// were added, so this file was missing. Intentionally minimal/no-op for
// now — extend here if future tests need shared env/mocks.
export {};
